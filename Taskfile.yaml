version: '3'

tasks:
  install:
    desc: Install Quirks (skills) from this repo
    silent: true
    cmds:
      - mkdir -p ~/.agents/skills
      - |
        for skill_path in skills/*; do
          if [ -d "$skill_path" ]; then
            skill_name=$(basename "$skill_path")
            rm -rf ~/.agents/skills/"$skill_name"
            cp -r "$skill_path" ~/.agents/skills/
          fi
        done

  list:
    desc: List Quirks (skills) present in this repo and their version
    silent: true
    cmds:
      - |
        (
          echo "| Skill | Repo Version | Installed Version | Status |"
          echo "| --- | --- | --- | --- |"
          for file in skills/*/SKILL.md; do
            frontmatter=$(sed -n '2,/^---$/ { /^---$/!p; }' "$file")
            name=$(echo "$frontmatter" | yq '.name')
            version=$(echo "$frontmatter" | yq '.metadata.version')
            
            installed_file="$HOME/.agents/skills/$name/SKILL.md"
            if [ -f "$installed_file" ]; then
              installed_frontmatter=$(sed -n '2,/^---$/ { /^---$/!p; }' "$installed_file")
              installed_version=$(echo "$installed_frontmatter" | yq '.metadata.version')
            else
              installed_version="N/A"
            fi
            
            if [ "$version" = "$installed_version" ]; then
              status="Up-to-date"
            else
              status="Mismatch (run task install) ⚠️"
            fi
            
            echo "| $name | $version | $installed_version | $status |"
          done
        ) | glow -

  update-readme:
    desc: Regenerate and update the skills table in README.md
    silent: true
    cmds:
      - |
        temp_table=$(mktemp)
        echo "| Skill | Version | Description |" >> "$temp_table"
        echo "| --- | --- | --- |" >> "$temp_table"
        for file in skills/*/SKILL.md; do
          frontmatter=$(sed -n '2,/^---$/ { /^---$/!p; }' "$file")
          name=$(echo "$frontmatter" | yq '.name')
          version=$(echo "$frontmatter" | yq '.metadata.version')
          description=$(echo "$frontmatter" | yq '.description')
          echo "| [$name](skills/$name) | \`$version\` | $description |" >> "$temp_table"
        done
        
        # Insert table into README.md between markers
        temp_readme=$(mktemp)
        export TEMP_TABLE="$temp_table"
        awk '
          /<!-- SKILLS_TABLE_START -->/ {
            print
            print ""
            system("cat \"" ENVIRON["TEMP_TABLE"] "\"")
            print ""
            skip = 1
            next
          }
          /<!-- SKILLS_TABLE_END -->/ {
            skip = 0
          }
          !skip {
            print
          }
        ' README.md > "$temp_readme"
        mv "$temp_readme" README.md
        rm -f "$temp_table"
